server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/denis-zhdanov/kotlin-baker"
          searchPaths: "repo/{application}/{profile},repo/{application}"